import styled from '@emotion/styled';

export const SceneAddFormCloseButtonWrapper = styled.button`
  position: absolute;
  top: -2.4rem;
  right: 0;
  border: 0;
  background-color: transparent;
  outline: none;
`;

SceneAddFormCloseButtonWrapper.defaultProps = {
  type: 'button',
};
