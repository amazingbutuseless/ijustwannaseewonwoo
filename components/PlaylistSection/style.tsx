import styled from '@emotion/styled';

export const Wrapper = styled.section`
  margin-bottom: 3rem;

  & > h2 {
    margin: 0;
    padding: 0;
    text-indent: -1000rem;
  }

  .slick-current {
    z-index: 1;
  }
`;
